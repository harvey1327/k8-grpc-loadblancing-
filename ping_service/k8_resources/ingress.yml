apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ping
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ping
            port:
              number: 8080